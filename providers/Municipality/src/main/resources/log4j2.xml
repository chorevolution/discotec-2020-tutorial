<?xml version="1.0" encoding="UTF-8"?>
<Configuration>

    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${hostName} --- [%15.15t] %-40.40c{1.} : %m%n%ex</Property>
    </Properties>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${LOG_PATTERN}" />
        </Console>

        <RollingFile name="RollingFile" fileName="logs/municipality.log" filePattern="logs/municipality-%d{yyyy-MM-dd}-%i.log.gz">
            <Policies>
                <SizeBasedTriggeringPolicy size="30MB" />
            </Policies>
            <PatternLayout pattern="${LOG_PATTERN}" />
        </RollingFile>

    </Appenders>
    <Loggers>

        <Root level="debug" additivity="false">
            <AppenderRef ref="Console" />
        </Root>

        <!-- Log everything in Spring Core -->
        <Logger name="org.springframework" level="debug" additivity="false">
            <AppenderRef ref="Console" />
<!-- 
            <AppenderRef ref="RollingFile" />
 -->            
        </Logger>

    </Loggers>

</Configuration>